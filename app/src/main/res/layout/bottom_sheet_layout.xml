<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.bottomsheet.BottomSheetDragHandleView
        android:id="@+id/drag_handle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar_widget"
        android:layout_width="match_parent"
        android:layout_height="@dimen/list_dialog_toolbar_height"
        android:layout_marginTop="@dimen/list_dialog_toolbar_margin_top"
        android:background="@null" />

    <View
        android:id="@+id/breakpointsDiv"
        style="@style/HorizontalDivider" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/select_dialog_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/list_dialog_recycler_margin_top"
        android:clipToPadding="false"
        android:paddingHorizontal="16dp"
        android:paddingBottom="@dimen/list_dialog_list_margin_bottom"
        android:background="@color/transparent"
        android:divider="@null"
        android:overScrollMode="ifContentScrolls"
        android:scrollbarStyle="insideOverlay"
        android:scrollbars="vertical" />

</LinearLayout>